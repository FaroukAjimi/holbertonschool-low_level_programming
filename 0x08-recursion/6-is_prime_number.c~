#include "holberton.h"
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int primo(int i, int n)
{
	if (i <= 2)
		return (0);
	if (n % i == 0)
		return (1);
	else
		return (0);
}
int is_prime_number(int n)
{
	is_prime_number(primo(i , n / i));
}
