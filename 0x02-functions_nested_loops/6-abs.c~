#include "holberton.h"
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _abs(int x)
{
	if (x >= 0)
		return (1);
	if (x < 0)
		return (x * -1);
	return (0);
}
