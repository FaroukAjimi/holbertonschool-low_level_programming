#include <stdio.h>
/**
 *
 *
 */
int main()
{
	int i,y;
	for (i = 0; i <= 9; i++)
	{
		for (y = 0; y <= 8; y++)
		{
			putchar(i + '0');
			putchar(y + '0');
		}
		putchar(' ');
		putchar(',');
	}

}
