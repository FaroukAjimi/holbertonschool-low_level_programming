#include "holberton.h"
/**
 * print_line - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void print_line(int n)
{
	if (n <= '0')
	_putchar ('\n');
	for (n = 1 ; n > 0 ; n++)
	{
		_putchar('_');
	}
	_putchar('\n');
}
